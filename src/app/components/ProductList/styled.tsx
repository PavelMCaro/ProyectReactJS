import styled from "styled-components";

export const Wrapper = styled.div`
    width: auto;
    position: relative;
    top: 100px;
    display: flex;
    flex-direction: row;
    flex-wrap: wrap;
`;